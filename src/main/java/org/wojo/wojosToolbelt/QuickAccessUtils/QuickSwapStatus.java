package org.wojo.wojosToolbelt.QuickAccessUtils;

// These are values tied to the player not the Item. View 
public class QuickSwapStatus {
    boolean isEnabled = false;  // The hotbar button that opens the GUI
    int equippedPosition = 8;   // Hotbar location that quick access items need to be placed in/ Button used to open UI
    int targetPosition = 0;     // Where items get quickswapped into
}